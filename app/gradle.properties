# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Added Windows-specific options to prevent file locking issues
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8 -XX:+UseG1GC -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Duser.country=US -Duser.language=en

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
android.nonFinalResIds=false

# ============================================
# PERMANENT FIX FOR FILE LOCKING ISSUES
# ============================================

# Enable Gradle daemon (faster builds, but can cause locking - we'll manage it)
org.gradle.daemon=true

# Enable build cache for faster incremental builds
org.gradle.caching=true

# Enable configuration on demand (only configure projects that are needed)
org.gradle.configureondemand=true

# Enable file system watching for faster builds (Gradle 7.0+)
org.gradle.vfs.watch=true

# Maximum idle time for daemon (stops daemon after 3 hours of inactivity)
org.gradle.daemon.idletimeout=10800000

# Enable build scan for better diagnostics (optional)
# org.gradle.scan=true

# Windows-specific: Reduce file locking by using NIO file system
# This helps prevent "file is being used by another process" errors
org.gradle.unsafe.configuration-cache=false

# Enable worker API for parallel task execution
org.gradle.workers.max=4

# Disable file system case sensitivity checks (Windows doesn't care)
org.gradle.caseSensitiveFileSystem=false
