# name: CD

# on:
#   workflow_run:
#     workflows: ["CI"]
#     types:
#       - completed
#     branches:
#       - main
#       - develop
#   workflow_dispatch:
#     inputs:
#       environment:
#         description: 'Deployment environment'
#         required: true
#         default: 'production'
#         type: choice
#         options:
#           - production
#           - staging

# env:
#   IMAGE_NAME: ${{ secrets.DOCKER_HUB_USERNAME }}/v8id-cloud-backend

# jobs:
#   deploy:
#     name: Deploy to OCI
#     runs-on: ubuntu-latest
#     if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Login to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKER_HUB_USERNAME }}
#           password: ${{ secrets.DOCKER_HUB_TOKEN }}

#       - name: Pull latest image
#         run: |
#           docker pull ${{ env.IMAGE_NAME }}:latest || true

#       - name: Build and push to Docker Hub
#         if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           file: ./backend/Dockerfile
#           push: true
#           tags: |
#             ${{ env.IMAGE_NAME }}:latest
#             ${{ env.IMAGE_NAME }}:${{ github.sha }}
#             ${{ env.IMAGE_NAME }}:${{ github.ref_name }}
#           cache-from: type=registry,ref=${{ env.IMAGE_NAME }}:buildcache
#           cache-to: type=registry,ref=${{ env.IMAGE_NAME }}:buildcache,mode=max

#       - name: Setup SSH
#         run: |
#           mkdir -p ~/.ssh
#           echo "${{ secrets.OCI_COMPUTE_SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
#           chmod 600 ~/.ssh/id_rsa
#           ssh-keyscan -H ${{ secrets.OCI_COMPUTE_IP }} >> ~/.ssh/known_hosts

#       - name: Deploy to Compute Instance
#         run: |
#           ssh -o StrictHostKeyChecking=no ${{ secrets.OCI_COMPUTE_SSH_USER }}@${{ secrets.OCI_COMPUTE_IP }} << EOF
#             set -e
            
#             # Export Vault OCID for secret fetching
#             export VAULT_OCID="${{ secrets.OCI_VAULT_OCID }}"
#             export ENV_FILE="/opt/v8id-cloud/.env"
#             export PROJECT_NAME="v8id-cloud"
            
#             # Fetch secrets from Vault
#             echo "[INFO] Fetching secrets from OCI Vault..."
#             if [ -f /opt/v8id-cloud/scripts/fetch-secrets-from-vault.sh ]; then
#               chmod +x /opt/v8id-cloud/scripts/fetch-secrets-from-vault.sh
#               /opt/v8id-cloud/scripts/fetch-secrets-from-vault.sh || echo "[WARN] Failed to fetch from Vault, using existing .env if present"
#             else
#               echo "[WARN] Vault fetch script not found, using existing .env file"
#             fi
            
#             # Verify .env file exists
#             if [ ! -f "$ENV_FILE" ]; then
#               echo "[ERROR] .env file not found. Please create it manually or ensure Vault secrets are configured."
#               exit 1
#             fi
            
#             # Login to Docker Hub
#             echo "${{ secrets.DOCKER_HUB_TOKEN }}" | docker login -u '${{ secrets.DOCKER_HUB_USERNAME }}' --password-stdin
            
#             # Pull latest image
#             docker pull ${{ env.IMAGE_NAME }}:latest
            
#             # Stop and remove old container
#             docker stop v8id-backend || true
#             docker rm v8id-backend || true
            
#             # Run database migrations
#             docker run --rm \
#               --env-file $ENV_FILE \
#               ${{ env.IMAGE_NAME }}:latest \
#               pnpm prisma migrate deploy || echo "Migration failed or no migrations to run"
            
#             # Start new container
#             docker run -d \
#               --name v8id-backend \
#               --restart unless-stopped \
#               -p 4000:4000 \
#               --env-file $ENV_FILE \
#               ${{ env.IMAGE_NAME }}:latest
            
#             # Clean up old images
#             docker image prune -af --filter "until=24h" || true
#           EOF

#       - name: Health Check
#         run: |
#           sleep 10
#           for i in {1..30}; do
#             if curl -f http://${{ secrets.OCI_COMPUTE_IP }}:4000/health; then
#               echo "✅ Health check passed"
#               exit 0
#             fi
#             echo "Waiting for service to be healthy... ($i/30)"
#             sleep 5
#           done
#           echo "❌ Health check failed"
#           exit 1

#       - name: Deployment Summary
#         if: always()
#         run: |
#           echo "## 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "- **Environment**: ${{ github.event.inputs.environment || 'production' }}" >> $GITHUB_STEP_SUMMARY
#           echo "- **Image**: \`${{ env.IMAGE_NAME }}:${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
#           echo "- **Compute Instance**: \`${{ secrets.OCI_COMPUTE_IP }}\`" >> $GITHUB_STEP_SUMMARY
#           echo "- **Status**: ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "### Deployment Steps:" >> $GITHUB_STEP_SUMMARY
#           echo "1. ✅ Logged in to Docker Hub" >> $GITHUB_STEP_SUMMARY
#           echo "2. ✅ Built and pushed Docker image" >> $GITHUB_STEP_SUMMARY
#           echo "3. ✅ Connected to compute instance" >> $GITHUB_STEP_SUMMARY
#           echo "4. ✅ Fetched secrets from OCI Vault" >> $GITHUB_STEP_SUMMARY
#           echo "5. ✅ Pulled latest image" >> $GITHUB_STEP_SUMMARY
#           echo "6. ✅ Stopped old container" >> $GITHUB_STEP_SUMMARY
#           echo "7. ✅ Ran database migrations" >> $GITHUB_STEP_SUMMARY
#           echo "8. ✅ Started new container" >> $GITHUB_STEP_SUMMARY
#           echo "9. ✅ Performed health check" >> $GITHUB_STEP_SUMMARY
